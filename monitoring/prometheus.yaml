global:
  scrape_interval: 5s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
        - prometheus:9090

  - job_name: snackquest-backend
    static_configs:
      - targets:
        - backend:5000
    metrics_path: /metrics
